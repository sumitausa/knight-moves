import React from 'react';

import { withFirebase } from './Firebase';

class EditPage extends React.Component {
  state = {};

  render() {
    return <div>It Me</div>;
  }
}

export default withFirebase(Brookline);
